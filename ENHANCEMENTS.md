# Enhancements — par-rt-db (retired)

> **This file is retired.** The enhancement backlog now lives on the project kanban board
> (the `projects` kanban whose data/code live in `~/Repos/projects`, live at projects.pardev.net),
> tagged `enhancement`. The `/enhancement-next`, `/enhancement-all`, and `/enhancement-done`
> commands read and write the board, not this file.
>
> Historical context: this file was the original hand-authored enhancement candidate list
> (ENH-001 … ENH-015). ENH-001 through ENH-015 all shipped; ENH-016 (HTTP Range on storage)
> and ENH-017 (signed storage URLs) shipped later and were never added here. The `ENH-*` ids
> are preserved on the board and in `docs/superpowers/plans/*-enh-NNN-*.md` filenames, so old
> citations still resolve. New enhancement ideas are filed as `enhancement`-tagged board cards
> via `/enhancement-create` rather than appended here.

For the current shipped feature set, see `FEATURE_MATRIX.md` (the Convex-parity contract) and
`CHANGELOG.md`. For per-feature design/rationale, see `docs/superpowers/specs/` and the
`docs/opus/` enhancement implementation plans.
