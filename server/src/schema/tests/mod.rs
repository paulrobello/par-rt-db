//! Unit tests for the `schema` module, split by topic (QA-005). Each file
//! below `use crate::schema::*;` plus whatever crate-external types its tests
//! reference directly.

mod computed_tests;
mod structure_tests;
mod value_and_search_tests;
mod vector_owner_collaborators_tests;
mod widening_ttl_authorize_filter_tests;
