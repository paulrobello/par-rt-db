{
  "name": "aggregate-int64-avg-number",
  "$comment": "AVG over an int64 field stays a JSON number — the documented f64 form (a fractional mean has no int64 representation, so the decimal-string convention cannot apply). Small values keep the comparison float-exact: 3 and 4 average to 3.5 while sum in the same query pins the string convention.",
  "schema": {
    "tables": {
      "balances": {
        "fields": {
          "acct": { "type": "string" },
          "sats": { "type": "int64" }
        },
        "indexes": [{ "name": "by_acct_sats", "fields": ["acct", "sats"] }]
      }
    }
  },
  "seed": [
    { "acct": "b", "sats": "3" },
    { "acct": "b", "sats": "4" }
  ],
  "op": {
    "query": {
      "table": "balances",
      "index": "by_acct_sats",
      "eq": ["b"],
      "aggregate": { "aggregates": { "s": "sum", "a": "avg" } }
    }
  },
  "expect": { "s": "7", "a": 3.5 }
}
