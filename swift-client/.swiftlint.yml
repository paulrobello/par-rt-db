included:
  - Sources
  - Tests
line_length:
  warning: 120
identifier_name:
  min_length: 2
  excluded:
    - k # HybridSearchQuery.k — the wire field name (the RRF constant), not style
type_body_length: 1600 # WsClientTests.swift + WsClient.swift accumulate Tasks 12+13+14 suites/surfaces (+ fix-round, presence, and optimistic tests) in one file per the task layout
file_length: 2500 # WsClientTests.swift accumulates Tasks 12+13+14 suites (+ fix-round, onTermination-release, presence, and optimistic tests) in one file per the task layout; InMemoryEngine.swift grew past 2300 with the awaitSignal engine branch (2026-08-21)
